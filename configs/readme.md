# Config Documents
This folder contains `.yaml` config files of the unsolved PDE problem, training & validation dataset, training methodology and solver. Those `.yaml` files can be loaded by `utils.cfg_util.load_config`


